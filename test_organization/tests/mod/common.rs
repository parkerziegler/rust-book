// Functions in subdirectories of the tests directory are not included
// in test reporting output. These functions are not compiled as a separate
// crate, unlike integration test modules.
pub fn setup() {}
